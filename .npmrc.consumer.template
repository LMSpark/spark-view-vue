# SPARK-VIEW 私有包 registry 配置模板
# 使用方法：
#   1. 复制此文件到你的项目根目录，重命名为 .npmrc
#   2. 将 REGISTRY_URL 替换为实际 Verdaccio 地址（如 http://192.168.1.100:4873）

@spark-view:registry=http://localhost:4873

# 如果 registry 需要登录（access: $authenticated），先执行：
# npm login --registry http://localhost:4873
# 然后将生成的 _authToken 填入以下条目（或由 npm login 自动写入）：
# //localhost:4873/:_authToken=YOUR_AUTH_TOKEN
