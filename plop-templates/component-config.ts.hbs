import type { ComponentContext } from '@spark-view/spark-component'

/**
 * {{pascalName}} 组件 config prop 类型
 *
 * {{description}}
 *
 * 遵循 demo/UserGrid.vue 模式：使用 Partial<ComponentContext> 而非自定义接口。
 * 如需扩展额外 props，通过 props.config.props?.['key'] 运行时访问。
 */
export type {{pascalName}}Config = Partial<ComponentContext>