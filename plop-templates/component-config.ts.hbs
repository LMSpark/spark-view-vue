import type { SparkNode } from '@spark-appworks/spark-component'

/**
 * {{pascalName}} 组件配置局部类型
 *
 * {{description}}
 *
 * 和所有 SPARK 组件一样使用 SparkNode，额外 props 通过 config.props['key'] 访问。
 */
export type {{pascalName}}Config = SparkNode
