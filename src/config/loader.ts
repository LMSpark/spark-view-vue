/**
 * @deprecated 已迁移至 @spark-view/spark-app
 * 此文件保留为向后兼容的 re-export barrel。
 * 新代码请直接 import { ConfigLoader, TenantResolver, loadAppConfig } from '@spark-view/spark-app'
 */
export { ConfigLoader, TenantResolver, loadAppConfig } from '@spark-view/spark-app'
