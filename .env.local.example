# ── SPARK View 本地环境变量（不提交到 git）────────────────────────────────────
#
# 使用说明：
#   1. 复制本文件为 .env.local（已在 .gitignore 中）
#   2. 填入真实配置后重启 Vite 开发服务器
#
# ─────────────────────────────────────────────────────────────────────────────

# ── AI 后端地址（启用 Java 服务时取消注释）────────────────────────────────────
# 启用后，所有 POST /api/ai/chat 请求将转发到 Java 服务，而不是使用 Vite mock。
# Java 服务启动命令（在 spark-ai-server/ 目录）：
#   $env:OPENAI_API_KEY = "sk-..."
#   mvn spring-boot:run
#
# AI_BACKEND_URL=http://localhost:8080
