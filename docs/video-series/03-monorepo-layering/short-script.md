# 第 03 集短视频稿

目标时长：90-180 秒  
短视频标题：Monorepo 不是目录多，是边界清楚

## 口播稿

配置化平台最怕什么？不是文件多，而是所有东西都堆在一个应用里。

SPARK_VIEW 的 monorepo 做的是分层：根应用负责集成，组件包负责解释运行时，数据包负责 DataSet，配置包负责加载编译，AI 包负责通用协议和业务模块接入。

这样做的关键不是“拆目录”，而是控制依赖方向。

AI core 不吃业务语义，组件运行时不变成数据层，根应用不沉淀通用内核。

完整源码链路看本集长视频。
