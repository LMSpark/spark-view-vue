# 第 02 集短视频稿

目标时长：90-180 秒  
短视频标题：为什么 SPARK_VIEW 要拆成四个文件？

## 口播稿

把整个页面塞进一个巨大 JSON，看起来省事，长期一定难维护。

SPARK_VIEW 选择拆成四文件：`rule.json` 管组件树，`pagedata.json` 管数据空间，`script.js` 管行为扩展，`style.css` 管样式资产。

这不是形式主义。拆开以后，加载、编译、预览、回滚、AI 修改和代码审查都有了边界。

页面不再是一坨配置，而是一组可以被治理的生产资料。

完整源码链路看本集长视频。
