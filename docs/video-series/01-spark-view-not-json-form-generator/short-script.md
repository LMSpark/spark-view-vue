# 第 01 集短视频稿

目标时长：90-180 秒  
短视频标题：别再叫 SPARK_VIEW JSON 表单

## 口播稿

如果一个后台页面只有输入框和提交按钮，JSON 表单够用。

但一旦它有主从表、树数据、权限快照、聚合、脚本、预览、回滚，问题就不是“怎么少写 Vue”，而是“怎么管理页面这个长期资产”。

SPARK_VIEW 的判断很直接：页面应该被资产化。结构、数据、行为、样式拆成四类文件，再由稳定运行时解释。

所以它不是表单生成器，也不是一次性代码生成器。它是一套让页面可治理、可审计、可预览、也能被 AI 安全修改的工程系统。

完整源码链路看本集长视频。
