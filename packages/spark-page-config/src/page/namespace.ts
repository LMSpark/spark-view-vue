/**
 * SparkPageConfig 命名空间 — createConfigLoader 的命名空间入口
 */

import { createConfigLoader } from './loading/page-config-loader'

export const SparkPageConfig = {
  createLoader: createConfigLoader,
  createConfigLoader,
}
