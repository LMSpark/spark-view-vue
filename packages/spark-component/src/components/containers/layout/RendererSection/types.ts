/**
 * @module @spark-appworks/spark-component:components/containers/layout/RendererSection/types
 * RendererSection 模块，属于 SPARK component container/layout-container。
 * 组件目录: containers/layout。
 * 导出 ClassModel symbol: RendererSectionApi（共 1 个 symbol）。
 */
/** Renderer Section Api 的语义模型。 */
export type RendererSectionApi = {
  isCollapsed(): boolean
  setCollapsed(value: boolean): void
  toggle(): void}
