/**
 * @module @spark-appworks/spark-component:components/fields/data-components/FieldColor.props
 * FieldColor 模块，属于 SPARK component field-level/data-field。
 * 组件目录: fields/data-components。
 * 导出 ClassModel symbol: RColorProps（共 1 个 symbol）。
 */
import type { SparkFieldSemanticProps, SparkNodeProps } from '../../shared-types'

/** RColor Props 的属性契约。 */
export type RColorProps = SparkNodeProps & SparkFieldSemanticProps<string>
