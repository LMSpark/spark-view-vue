/**
 * @module @spark-appworks/spark-component:components/fields/data-components/FieldFilePath.props
 * FieldFilePath 模块，属于 SPARK component field-level/data-field。
 * 组件目录: fields/data-components。
 * 导出 ClassModel symbol: RFilePathProps（共 1 个 symbol）。
 */
import type { SparkFilePickerFieldProps, SparkNodeProps } from '../../shared-types'

/** RFile Path Props 的属性契约。 */
export type RFilePathProps = SparkNodeProps & SparkFilePickerFieldProps
