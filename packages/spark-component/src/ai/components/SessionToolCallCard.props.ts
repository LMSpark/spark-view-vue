/**
 * @module @spark-appworks/spark-component:ai/components/SessionToolCallCard.props
 * @spark-appworks/spark-component 的 ai/components/SessionToolCallCard.props 模块。
 * 导出 ClassModel symbol: SessionToolCallCardProps（共 1 个 symbol）。
 */
import type { ToolCallDisplayItem } from '../types'

/** Session Tool Call Card Props 的属性契约。 */
export type SessionToolCallCardProps = Readonly<{
  toolCall: ToolCallDisplayItem
}>
