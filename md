应该统一一个组件上下包装，能告知子，我是谁，我有什么能力，子只负责消费，子可以沿着上下文向上追踪，不是搞一堆能力键，数据域能力键是为 组件层级与数据层级不一致而设计的
全部纠正，迭代修改，不考虑向后兼容兼容，架构层面统一处理除外，处理前必须先经过我的审核同意，不再个例做任何放款处理，遵循SPARK的理念点，严格遵循父不知道子是谁，也不操作子，父唯一能控制的子渲染的区域，父主动提供能力供子使用，子随父智能渲染的思路，严格遵循SOLID
深度剖析packages/spark-component结构， 找到那没有遵循SPARK的理念点，严格遵循父不知道子是谁，也不操作子，父唯一能控制的子渲染的区域，父主动提供能力供子使用，子随父智能渲染的思路，严格遵循SOLID，