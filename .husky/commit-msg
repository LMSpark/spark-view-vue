#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Enforce Conventional Commits via commitlint (explicit config to avoid JS/ESM loader issues)
pnpm exec -- commitlint --config commitlint.config.cjs --edit "$1" || exit 1

